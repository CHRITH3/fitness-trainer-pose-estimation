# Core dependencies
flask>=2.0.0
opencv-python>=4.5.0
mediapipe==0.10.9
numpy>=1.21.0
# Video processing (H.264 codec support for browser-compatible videos)
imageio>=2.31.0
imageio-ffmpeg>=0.4.8

# LLM integration (OpenAI-compatible API for Qianwen)
openai>=1.0.0

# Optional: For better chart rendering in dashboard (included via CDN)
# matplotlib>=3.5.0
