# 2026-04-20 去健身化执行说明

## 任务目标

将仓库从“健身 + 蹦床混合语义”收敛为“蹦床单产品语义”，同时保留已有蹦床分析能力与稳定公共路由：
- `/`
- `/dashboard`
- `/profile`
- `/video_analysis`

## 本次处理内容

### 1. 后端与运行时
- 重写 `app.py`，删除健身启动依赖与健身 API 路由
- 保留并聚焦蹦床分析相关 API
- 明确 `/api/video/upload` 仅接受 `exercise_type=trampoline`
- 删除 `/api/video/analyze_frame` 依赖路径

### 2. 前端页面
- 首页改为蹦床实时页预留壳层
- dashboard 改为蹦床看板占位页
- profile 改为蹦床训练档案占位页
- video_analysis 改为 trampoline-only 页面，移除 fitness selector / fitness nav / fitness upload path

### 3. 文档
- README 改写为蹦床产品说明
- CLAUDE.md 同步为 trampoline-only 架构与命令
- 增加本执行文档记录迁移过程

## 从被删除内容中提炼出的可复用思路

### 可学习 / 可复用的地方
1. **页面骨架复用**
   - dashboard / profile 原本的卡片式布局、图表容器和双栏结构，仍然适合后续蹦床项目使用。

2. **上传 + 状态轮询链路**
   - 视频分析页已有“上传 -> 后端处理 -> 轮询状态 -> 展示报告”的完整链路，这比重新设计更值得保留。

3. **配置与分层意识**
   - 旧系统虽然是健身语义，但前后端、分析引擎、页面模板、报告逻辑的分层方式对蹦床项目仍有参考价值。

4. **报告 / AI 解释层**
   - AI 分析的分段展示卡片、流式返回、缓存思路，可继续作为蹦床分析解释层基础。

### 本次不再保留的部分
- 健身动作定义与 YAML 配置体系
- 实时健身相机流
- 健身动作计数与 form score 逻辑
- 健身 profile/workout 语义与接口

## 当前保留的“备用结构”

### 前端备用
- dashboard 图表容器
- profile 卡片式信息结构
- video analysis 的日志区、报告区、AI 区

### 后端备用
- Flask 路由组织方式
- 独立子进程视频处理模式
- 视频上传、状态轮询、产物回放的基本模式

## 后续建议

1. 在 dashboard 中接入：
   - 总跳次
   - 动作分布
   - 落点热区
   - 训练历史摘要

2. 在 profile 中接入：
   - 运动员档案
   - 器材/床面配置
   - 训练偏好
   - AI 观察记录

3. 如果后续还想继续整理：
   - 再做一轮更纯粹的命名与目录重构
   - 但建议在功能稳定后单独进行，不与业务改动混在一起

