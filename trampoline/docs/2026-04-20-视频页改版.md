# 2026-04-20 视频页改版执行说明

## 任务背景

本轮工作基于：

- `.omx/plans/ralplan-video-analysis-direct-calibration-and-panel-layout.md`
- `.omx/plans/prd-video-analysis-direct-calibration-and-panel-layout.md`
- `.omx/plans/test-spec-video-analysis-direct-calibration-and-panel-layout.md`

目标是在**不改变现有蹦床分析语义**的前提下，解决视频分析页两个核心问题：

1. 单帧标定必须滚动到下方重复画面，交互割裂。
2. 右侧面板过长，桌面端需要频繁滚轮拖动才能看到关键信息。

## 本次实现结果

### 1. 标定改为直接在主视频画面上完成

- 上传并进入待标定态后，不再默认激活一个“首帧草稿”。
- 用户可以先在主视频里：
  - 播放
  - 暂停
  - 拖动进度
- 点击 **“添加当前帧标定”** 后，才会在主视频上方开启可点击 overlay。
- 用户直接在上方暂停视频画面上点击四个角点完成标定。
- 下方区域仅保留：
  - 操作按钮
  - 说明文案
  - 标定 chips
  - 关键帧列表

### 2. 去掉下方重复标定画面

- 不再显示单独的下方 `corner-canvas` 标定图。
- 主视频与标定交互使用同一视觉表面，避免“跳到另一张图上去点”的感觉。

### 3. 右侧面板重排为桌面优先的紧凑布局

右侧改为双列信息区：

- 第一列：
  - 实时统计
  - 落点图
- 第二列：
  - 分析报告
  - AI 分析
  - 过程反馈

这样能在常见桌面浏览器高度下更集中地展示关键信息。

## 实时统计调整

实时统计缩减为 4 项：

1. 跳次
2. 当前跳滞空时间
3. 动作
4. 落点坐标 + conf

其中：

- 如果当前处于 flight，则优先显示当前实时数据；
- 如果当前没有有效实时值，则回退到**最近一次有效跳**的数据；
- 落点图与视频侧的落点数据来源保持同一家族语义。

## 分析报告与反馈调整

### 分析报告

默认只显示摘要：

- 分析类型
- 总跳次
- 动作分布
- 处理耗时

逐跳明细被折叠到按钮 **“每跳详细数据”** 下。

### 过程反馈

- 高度压缩为短滚动区
- 页面上优先看见最新几条
- 避免反馈区域把整页撑长

## 后端配套调整

为支持新的紧凑 UI，状态接口新增了**加法式字段**，未改原有跳跃分割/落地语义：

- `phase`
- `current_flight_frames`
- `current_flight_duration_s`
- `latest_landing`
- `landings`
- `fps`
- `video_fps`

这些字段只用于让前端正确展示“当前值 / 最近一次有效值”，不改变既有分析结果定义。

## 代码结构整理

本轮还顺手做了一次小范围整理，避免视频页逻辑继续堆在一个文件里：

- 新增 `static/js/video_analysis_helpers.js`
  - 负责 compact stats 和 latest-valid fallback 纯逻辑
- `static/js/trampoline_calibration_ui.js`
  - 负责直接标定的视频 overlay 控制器
- `static/js/video_analysis.js`
  - 继续负责页面轮询、报告、落点图、AI 分析等主流程

## 回归与验证

本轮新增/更新了以下保护：

- `tests/test_trampoline_api.py`
- `tests/test_trampoline.py`
- `tests/test_trampoline_frontend_contract.py`
- `tests/test_trampoline_calibration_ui.mjs`
- `tests/test_video_analysis_ui_helpers.mjs`

通过验证：

```bash
python -m pytest tests -q
node tests/test_trampoline_calibration_ui.mjs
node tests/test_video_analysis_ui_helpers.mjs
python -m py_compile app.py video_processor.py $(find trampoline -name '*.py' -print)
git diff --check
python -m ruff check app.py video_processor.py trampoline tests
```

结果：

- `85 passed`
- Node 侧 UI helper / calibration tests passed
- `py_compile` 通过
- `git diff --check` 通过
- `ruff check` 通过

## 额外说明

本轮还专门修复了两个容易被忽略的交互问题：

1. **overlay 不会在待标定一进入时就挡住视频控件**
   - 只有在用户明确点击“添加当前帧标定”后才激活可点击层。

2. **不会默认拿 frame 0 当作可误保存草稿**
   - 草稿必须由用户显式选择当前帧后才建立。

## 后续建议

如果下一轮还要继续优化视频分析页，优先级建议如下：

1. 增加真实浏览器人工点测（不同桌面分辨率）
2. 如果落点图后续还会复用，可继续抽出独立 renderer
3. 如果后续还会扩展更多右侧信息，建议继续保持“摘要优先、详情折叠”原则
