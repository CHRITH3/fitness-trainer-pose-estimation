# E2E Test Skeleton

Phase 0 keeps browser-facing coverage lightweight. The current `/trampoline` smoke path uses Flask's test client as the repo's browser-equivalent scaffold, which avoids adding a second runtime stack before the trampoline UI is interactive.
